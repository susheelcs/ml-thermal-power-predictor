# Notebooks

The production experiment is implemented as Python modules under `src/` so it
can be tested and reproduced from the command line. A notebook can be added
later for interactive exploratory analysis without duplicating the training
logic.
