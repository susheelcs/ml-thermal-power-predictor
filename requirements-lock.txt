# Exact versions used for the checked-in experiment artifacts in this build.
# pyarrow is required only for direct Parquet access/rebuilding; the default
# experiment input is the included gzip-compressed CSV.
numpy==2.3.5
pandas==2.2.3
scikit-learn==1.8.0
xgboost==3.1.3
matplotlib==3.10.8
joblib==1.5.3
pytest==8.4.2
jupyter==1.1.1
